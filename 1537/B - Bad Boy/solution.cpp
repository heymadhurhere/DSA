#include <bits/stdc++.h>
using namespace std;
 
void solve(long long n, long long m, long long i, long long j) {
    // put on opposite corners
    cout << 1  << " " << m << " " << n << " " << 1 << endl;
}
 
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        long long n, m, i, j;
        cin >> n >> m >> i >> j;
        solve(n, m, i, j);
    }
    return 0;
}